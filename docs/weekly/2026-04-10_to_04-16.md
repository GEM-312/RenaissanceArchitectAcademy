# Weekly Check-in — Apr 10–16, 2026

## Activity
- **13 commits**, **4 PRs merged**
- **+3,625 / −6,982** lines (net **−3,357**) — twice as many deletions as insertions
- All 4 PRs authored by Claude / GEM-312

## PRs merged
- **#1** Migrate to modern `@Observable` SwiftUI architecture (Apr 15)
- **#2** Hero animations for building-name transitions via `matchedGeometryEffect` (Apr 15)
- **#3** Renaissance design system — colors, buttons, theme support (Apr 15)
- **#4** Integrate `GameSettings` for theme-aware colors + card rendering optimization (Apr 16)

## Themes

### Architecture modernization
- `CityViewModel`, `MuseumSketchService`, `BirdChatViewModel`, `AIService` migrated to `@Observable`
- Hero animations added across 7 files

### Dead code purge (biggest cleanup of the project so far)
- Removed entire quiz/test system — replaced by knowledge cards (20 files, **−3,569 lines in one commit**) — dead code had been sitting since February
- Unified color theme + reusable components + material assets (63 files, −472 net)

### Dark mode / theme system
- Theme-aware dialog backgrounds, text, borders across 11 overlay views
- Memory optimization + dark mode polish pass (16 files)

### Content & UX
- NPC encounters wired in (historical figures)
- Leonardo bird persona
- Crafting room repositioning
- Furnace fire, scene transitions, UI component unification

### Stability fixes
- CityScene retain cycle — removed `[self]` captures from scene callbacks (commit `ceb32c5`)
- macOS crash fix
- `MascotDialogueView` parameter-order bug after hero-animation merge (commit `c728b32`)

## Takeaway
Cleanup-dominated week. Most visible product change: NPCs + Leonardo bird persona. Most important invisible change: `@Observable` migration + quiz-system removal.
