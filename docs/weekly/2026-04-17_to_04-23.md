# Weekly Check-in — Apr 17–23, 2026

## Activity
- **37 commits**, **5 PRs merged**
- **+9,687 / −5,363** lines (net **+4,324**) — nearly 3× the volume of the prior week
- Commits mostly by Marina; PRs from Claude / GEM-312

## PRs merged
- **#5** Reflect mode for architectural sketches + UI improvements (Apr 17)
- **#6** Apple Game Center integration (Apr 17)
- **#7** Weekly Health Check — brown emoji + Eye timer leak fixes (Apr 23)
- **#8** Refactor ApiPlan.md to reflect shipped work, narrow backlog to 4 items (Apr 23)
- **#9** Blueprint-based sketching + Swift 6 concurrency fixes (Apr 23)

## Themes

### Apr 17 — Game Center + TestFlight triage
- Game Center integration: auth, leaderboards, 13 science achievements, fixed IDs, proper dashboard presentation
- Wired Game Center Activities to station mini-games (iOS 26+)
- 7 TestFlight bugs fixed: layout clipping, sketch reflect mode, swipe hint, back button
- Workbench fix: show Mix button when tool already owned
- TestFlight feedback sync scripts + **repo-level** API key protection (gitignore + script guards). Note: **runtime proxy not done yet** — Claude key still in bundle, must move to Cloudflare Worker before App Store submission.

### Apr 18 — Sezione cross-section sketching (5 commits)
- New data model: 3-layer types, flow state machine, actions (`SezioneCanvasState`)
- `SezioneCanvasView` — 3-layer cross-section drawing canvas
- Wired into `SketchingChallengeView`
- Sezione content authored for Pantheon
- Infographic reward system — dust-reveal effect, inline on card (not fullscreen)
- Light mode card readability pass — darker, more opaque backgrounds

### Apr 21 — Phase 5 NPCs
- NPC helpers on-demand (auto-popup killed — on-demand only, hybrid architecture)
- Recipe book view (full interactive book with iBooks page curl)
- Sketching cleanup

### Apr 23 — PencilKit sketching redesign (biggest single-day push of the project)
- Full PencilKit redesign: `PencilCanvasView` + `PKToolPicker` with ruler, blueprint at 0% default / 60% on Peek hold
- `StudyModeView` full-screen cover with "Just Study Today" skip
- `BlueprintStudyView` — iPhone/macOS compact reader
- `ShapeSnap.swift` — custom hold-to-snap lines + circles (Apple Notes-style)
- Touch drift fix (PKCanvasView scroll-view offsetting pencil nib)
- Tool picker hides during Study Mode + on canvas teardown
- Save sketches to building notebook + `SketchEditorView` (load stored PKDrawing, annotate, delete)
- `NotebookEntryType.sketch` added, Sketches tab in NotebookView
- Swift 6 concurrency fixes (`@MainActor` on `AIService` protocol, `@Entry` macro, Task cancellation)

### Apr 23 — Foldable inventory bar
- `FoldableInventoryBar.swift` — PKToolPicker-style. Drag grip flips top↔bottom, chevron minimizes
- Persists across Workshop / Forest / Crafting Room via `@AppStorage`

### Apr 23 — Workshop + City map overhaul
- New 7000×5000 Midjourney terrain art (Terrain_buildings.png + WorkshopTerrain.png)
- Workshop waypoint graph simplified: 65 → 17 nodes, ~100 → 31 edges
- Workshop station coords updated to match new terrain (all 10 stations)
- `CityScene.mapSize` standardized to 3500×2500 (was 4048×2144)
- `.aspectFill` migration + camera fit math rewritten in 4 places
- Fixes for parchment-edge pan, vertical squash, tiny-map-in-void rendering
- Foldable inventory bar grip + chevron visibility fix (20pt semibold ochre)

## Takeaway
Content/feature week — opposite polarity of last week's cleanup. Most impactful: **PencilKit sketching redesign**, **Game Center integration**, **Sezione sketching phase**. This was the week the app went from "sketching prototype + partial social layer" to "ship-candidate."
